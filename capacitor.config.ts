import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.touchutility.IonicQrcodeTest',
  appName: 'QrcodeTest',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
